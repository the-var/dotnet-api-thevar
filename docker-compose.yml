version: "3.9"

services:
  dotnet-api-thevar:
    build: .
    container_name: dotnet-api-thevar
    restart: always
    environment:
      - ASPNETCORE_URLS=http://+:8080
    ports:
      - "8081:8080"
